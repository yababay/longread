name: Deploy articles

on:
  push:
    branches:
      - gh

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 'latest'
      - run: npm install
      - name: Build the project
        run:  npx vite build
      - name: Deploy pages
        run:  npx gh-pages -d build -t true -u "Михаил Беляков <bigbelk@live.ru>"
